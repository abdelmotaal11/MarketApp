This is a basic Android application built to demonstrate and apply the core concepts of working with **RecyclerView**, **Adapter**, and **onClick** item handling in a list display.

## 📱 Features

- Displays a scrollable list using `RecyclerView`
- Binds data using a custom `Adapter`
- Handles click events on each item in the list
